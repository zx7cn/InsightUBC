{
	"title": "Filter courses with string field instead of number throws InsightError",
	"input": {
		"WHERE": {
			"EQ": {
				"courses_pass": "0"
			}
		},
		"OPTIONS": {
			"COLUMNS": [
				"courses_dept",
				"courses_avg",
				"courses_pass"
			]
		}
	},
	"errorExpected": true,
	"expected": "InsightError"
}
